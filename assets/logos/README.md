# Logos

Real product logos, stored locally so renders never depend on a CDN.

Fetch the tools YOU talk about:

```
python3 tools/logo-fetch.py notion notion 000000
python3 tools/logo-fetch.py canva canva 00C4CC
```

Slugs + brand hexes: https://simpleicons.org
